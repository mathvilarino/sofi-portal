---
phase: 2
slug: identidade-visual
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-27
---

# Phase 2 — UI Design Contract

> Visual and interaction contract for Phase 2: Identidade Visual.
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none (handcrafted Tailwind utilities) |
| Icon library | lucide-react 1.0.1 |
| Font (body/UI) | Inter (Google Fonts, variable: `--font-sans`) |
| Font (mono/code) | JetBrains Mono (Google Fonts, variable: `--font-mono`) |

**Source:** `app/layout.tsx`, `package.json`

No shadcn initialization performed. The project has an established handcrafted design system using Tailwind CSS 3 utility classes, custom CSS animations in `app/globals.css`, and CSS custom properties for brand tokens. This pattern must be preserved across all Phase 2 work.

---

## Design Tokens (Existing — Do Not Change)

These CSS custom properties are declared in `app/globals.css` and are the single source of truth for brand colors:

| Token | Value | Usage |
|-------|-------|-------|
| `--background` | `#030712` | Page background (gray-950 equivalent) |
| `--foreground` | `#f9fafb` | Primary text (gray-50) |
| `--sofix-brand` | `#6366f1` | SOFIX Engine accent (indigo-500) |
| `--sofi-brand` | `#b51d48` | SOFI Virtualization accent (rose-700 equivalent) |

**Source:** `app/globals.css` — locked, do not modify existing tokens.

---

## Spacing Scale

Declared values (all multiples of 4, following 8-point scale):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps (`gap-1`), inline padding, tag internal spacing |
| sm | 8px | Compact element spacing (`gap-2`, `px-2 py-0.5` on tags) |
| md | 16px | Default element spacing (`px-4`, `gap-4`) |
| lg | 24px | Section internal padding (`p-6`, `gap-6`) |
| xl | 32px | Card padding (`p-7`/`p-8` = 28–32px) |
| 2xl | 48px | Section vertical padding (`py-12`) |
| 3xl | 64px | Major section breaks (`py-24` = 96px is the current section padding) |

**Exceptions:**
- Hero section: `min-h-screen` full viewport — intentional, not a spacing token
- NavBar height: 64px (`h-16`) — fixed, touch-friendly
- Platform card logo container: 44px (`width={44} height={44}`) — icon-only touch target minimum
- Stats grid columns: `gap-6` (24px) — locked by current HeroSection implementation

**Source:** `components/sections/HeroSection.tsx`, `components/platform/PlatformCard.tsx`, `components/sections/NavBar.tsx`

---

## Typography

Exactly 4 declared roles. All sizes reference Inter (sans-serif) except where `font-mono` is noted.

### Canonical Weights

Two weights only:

| Weight Name | Value | Usage |
|-------------|-------|-------|
| Heavy | 900 (`font-black`) | Display wordmark, stat numbers, section headings — see per-role implementation notes |
| Regular | 400 (`font-normal`) | Body copy, all labels, utility text |

**Implementation note:** Tailwind classes `font-bold` (700), `font-semibold` (600), and `font-medium` (500) are permitted as subordinate implementation details within the Heavy role for heading hierarchy — they do not constitute independently declared weights. The contract weight is either Heavy (900) or Regular (400).

### Type Scale

| Role | Size | Weight | Line Height | Tailwind Classes | Usage |
|------|------|--------|-------------|-----------------|-------|
| Display | 72–144px (responsive) | Heavy (900) | 0.9 | `text-7xl sm:text-8xl lg:text-9xl font-black leading-[0.9]` | Hero "SOFI" wordmark only |
| Heading | 30–36px | Heavy (900) | 1.2 | `text-3xl sm:text-4xl font-black tabular-nums` for stat numbers; `text-3xl sm:text-4xl font-bold` for section titles (h2) | Section titles (h2), card titles (h3), AnimatedCounter output — same size range, unified role |
| Body | 14–18px | Regular (400) | 1.5–1.6 | `text-sm text-gray-400 leading-relaxed` / `text-lg font-bold` for card titles | Descriptions, card body copy, card titles (h3), card stat labels. **Label/Eyebrow variant:** `text-[10px] sm:text-xs font-semibold uppercase tracking-widest` — this is a Body modifier (uppercase + tracking-widest), not a separate size role |
| Utility | 11–12px | Regular (400) | 1.4 | `text-[11px] text-gray-600` / `text-[11px] font-mono` | Stat sublabels, URL footnotes, micro-context lines, platform URL display in PlatformCard. Mono variant uses `font-mono` (JetBrains Mono) at the same size |

**Phase 2 additions (required by HERO-01, POLISH-01):**
- Hero ecosystem headline uses weight 800 minimum per HERO-01 — implemented as `font-black` (Heavy role)
- Eyebrow labels use `text-xs font-semibold uppercase tracking-[0.2em]` — Body modifier, not a new role

**WCAG AA constraint (POLISH-02):** All informational body text must be `text-gray-400` minimum on `#030712` background. `text-gray-600` (`#4b5563`) fails contrast on `#030712` — reserved for decorative-only Utility text (stat sublabels, URL footnotes). Any copy that conveys information must use `text-gray-400` (`#9ca3af`) or lighter.

**Source:** `components/sections/HeroSection.tsx`, `components/platform/PlatformCard.tsx`, REQUIREMENTS.md POLISH-02, HERO-01

---

## Color

### 60 / 30 / 10 Distribution

| Role | Value | Tailwind | Usage |
|------|-------|----------|-------|
| Dominant (60%) | `#030712` | `bg-[#030712]` / `bg-background` | Page background, all full-bleed sections |
| Secondary (30%) | `rgba(255,255,255,0.03–0.06)` | `.glass` utility class | Cards (PlatformCard), NavBar (scrolled), section containers |
| Accent A (SOFIX) | `#6366f1` (indigo-500) | `text-indigo-400`, `bg-indigo-500/*`, `shadow-indigo-500/*` | SOFIX CTAs, SOFIX card border glow, SOFIX logo accent, SOFIX hero CTA button |
| Accent B (SOFI) | `#b51d48` / rose-500 | `text-rose-400`, `bg-rose-500/*`, `shadow-rose-500/*` | SOFI Virtualization CTAs, SOFI card border glow, SOFI hero CTA button |
| Destructive | `#f87171` (red-400) | `text-red-400` | StatusDot offline state only — no destructive actions in this phase |

**Accent reserved for (explicit list):**
- SOFIX Engine CTA button (indigo gradient)
- SOFIX Engine card border glow on hover (`boxShadow: rgba(99,102,241,0.15)`)
- SOFIX navbar link hover (`hover:text-indigo-400`)
- SOFI Virtualization CTA button (rose gradient)
- SOFI Virtualization card border glow on hover (`boxShadow: rgba(225,29,72,0.15)`)
- SOFI navbar link hover (`hover:text-rose-400`)
- Ecosystem logo SVG stroke (indigo — see BRAND-01)
- Eyebrow label icon tint (`text-indigo-400` for Sparkles icon in hero badge)

**Accent is NOT used for:** generic link underlines, form focus rings, loading spinners, decorative dividers.

**Ambient glow blobs (hero background):**
- Top-left: `bg-indigo-500/[0.04]` blur-3xl — SOFIX ambient
- Bottom-right: `bg-rose-500/[0.04]` blur-3xl — SOFI ambient
- These are decorative, must respect `prefers-reduced-motion` (POLISH-05)

**Source:** `app/globals.css`, `app/data/platforms.ts`, `components/sections/HeroSection.tsx`, REQUIREMENTS.md

---

## Component Inventory

All components that Phase 2 will create or modify:

### New Components

| Component | Path | Purpose | Requirement |
|-----------|------|---------|-------------|
| `EcosystemLogo` | `components/ui/EcosystemLogo.tsx` | SVG inline "S"/infinity logo with CSS stroke-dashoffset draw-on animation | BRAND-01, BRAND-02, BRAND-03 |
| `LazyMotionProvider` | `components/ui/LazyMotionProvider.tsx` | Wraps `LazyMotion` + `domAnimation` from framer-motion; must wrap any section using `motion.*` | BRAND-04 |

### Modified Components

| Component | Change | Requirement |
|-----------|--------|-------------|
| `NavBar.tsx` | Replace text "SOFI" + subtitle span with `<EcosystemLogo size="navbar" />` (24px) | BRAND-03 |
| `HeroSection.tsx` | Add `<EcosystemLogo size="hero" />` (80px) above headline; upgrade headline weight; add dual CTAs (indigo/rose per platform); add `LazyMotionProvider` wrapper if motion used | BRAND-04, HERO-01, HERO-02, HERO-03, HERO-04 |
| `PlatformsSection.tsx` | Add eyebrow label "PLATAFORMAS"; add hover elevation + border glow (already partially present — verify glow uses platform `glowColor`) | POLISH-01, POLISH-03 |
| `HowItWorksSection.tsx` | Add eyebrow label "COMO FUNCIONA" | POLISH-01 |
| `EcosystemFeaturesSection.tsx` | Add eyebrow label "ECOSSISTEMA" | POLISH-01 |
| `app/data/stats.ts` | `sublabel` field already exists — confirm all 4 stats have non-empty sublabel strings | POLISH-04 |
| `app/globals.css` | Add `@media (prefers-reduced-motion: reduce)` block pausing all `animation: * infinite` rules | POLISH-05 |
| `public/favicon.svg` | Replace with ecosystem logo SVG (same source as `EcosystemLogo`) | BRAND-03 |

### Unchanged Components

`AnimatedCounter`, `StatusDot`, `Section`, `SofixMockup`, `SofiMockup`, `PlatformCard` (hover glow already implemented), `TrustedBySection`, `ArchitectureSection`, `ComparisonSection`, `CtaSection`, `Footer`.

---

## Animation Contract

### EcosystemLogo Draw-On Animation

```
Technique: CSS stroke-dashoffset keyframe (not Framer Motion)
Why: Avoids hydration mismatch in static export (BRAND-02)
Property: stroke-dasharray = full path length; stroke-dashoffset animates from pathLength to 0
Duration: 1.5s ease-out, once on mount
prefers-reduced-motion: animation skipped, logo shows filled/stroked static state
```

### LazyMotion Setup (BRAND-04)

```
Import: LazyMotion + domAnimation from "framer-motion"
Placement: Wrap in layout.tsx OR at the top of each section that uses motion.*
Bundle cap: domAnimation ≈ 15 KB — do NOT import full `import { motion } from "framer-motion"`
Pattern: <LazyMotion features={domAnimation}><m.div ...></LazyMotion>
Use m.* (not motion.*) inside LazyMotion boundary
```

### Section Scroll Reveal (existing — preserve)

```
Pattern: Section.tsx IntersectionObserver → opacity-0 translate-y-8 → opacity-100 translate-y-0
Duration: 700ms transition
prefers-reduced-motion: must also respect this — add check in Section.tsx
```

### Card Hover (existing — verify)

```
PlatformCard hover: scale-[1.01] + boxShadow glow
Duration: 500ms transition-all
prefers-reduced-motion: scale/transform should be skipped; glow can remain
```

### Continuous Animations (must pause on prefers-reduced-motion)

```
.animate-pulse-glow (hero blobs) → pause
.animate-float / .animate-float-delayed (if used) → pause
.animate-gradient (if used) → pause
.animate-shimmer (if used) → pause
.status-dot::before (ping animation) → pause
```

**Reduced motion CSS block to add to globals.css:**
```css
@media (prefers-reduced-motion: reduce) {
  .animate-pulse-glow,
  .animate-float,
  .animate-float-delayed,
  .animate-gradient,
  .animate-shimmer,
  .status-dot::before {
    animation: none;
  }
}
```

---

## Copywriting Contract

| Element | Copy | Source |
|---------|------|--------|
| Primary CTA (SOFIX) | "Explorar SOFIX Engine" | HERO-03 / derived from current "Explorar plataformas" — split per platform |
| Primary CTA (SOFI) | "Explorar SOFI Virtualization" | HERO-03 |
| Hero subtitle (proof point) | One objective claim without jargon — e.g. "Virtualização e governança de dados em tempo real. Sem cópias. Sem ETL." | HERO-02 — must replace current generic "Virtualize, governe e consuma dados de qualquer fonte" with a concrete proof point |
| Platform section eyebrow | "PLATAFORMAS" | POLISH-01 / REQUIREMENTS.md |
| How it works eyebrow | "COMO FUNCIONA" | POLISH-01 / REQUIREMENTS.md |
| Ecosystem features eyebrow | "ECOSSISTEMA" | POLISH-01 / REQUIREMENTS.md |
| Stat micro-context lines | See `app/data/stats.ts` — all 4 sublabel strings already present | POLISH-04 — confirm non-empty |
| Empty state heading | Not applicable — portal is always populated (static data, no API-driven content) | — |
| Empty state body | Not applicable | — |
| Error state | Not applicable — no forms, no user input, no async data fetching in Phase 2 | — |
| Destructive confirmation | Not applicable — no destructive actions in Phase 2 | — |
| StatusDot offline | Existing: red dot ping animation — no copy, visual-only | — |

**Copywriting constraints:**
- All UI copy is in Brazilian Portuguese (pt-BR) — set by `app/layout.tsx` `lang="pt-BR"` and `REQUIREMENTS.md` out-of-scope list (no language toggle)
- Feature labels and UI controls may use English where it is the industry-standard term (e.g. "Virtual Databases", "CDC real-time", "Copy-on-Write")
- No generic marketing jargon in hero subtitle — must include at least one quantifiable proof point (HERO-02)

---

## Interaction States

| Element | Default | Hover | Active | Focus | Notes |
|---------|---------|-------|--------|-------|-------|
| PlatformCard | glass surface, no glow | scale-[1.01], platform glow shadow, gradient border opacity 100% | — | — | Already implemented — verify |
| NavBar links | `text-gray-500` | `text-white` | — | visible outline | `transition-colors` |
| NavBar platform links | `text-gray-500` | `text-indigo-400` / `text-rose-400` + platform bg tint | — | visible outline | Already implemented |
| Hero CTA (SOFIX) | indigo gradient, shadow-indigo-500/20 | indigo-500 gradient (lighter) | scale-95 | visible outline | `transition-all duration-300` |
| Hero CTA (SOFI) | rose gradient, shadow-rose-500/20 | rose-500 gradient (lighter) | scale-95 | visible outline | New addition |
| Secondary CTA ("Como funciona") | glass, `text-gray-400` | `text-white` | — | visible outline | Already implemented |
| EcosystemLogo (navbar) | static/post-draw state | no hover state in navbar | — | — | Draw-on plays once on load |
| EcosystemLogo (hero) | draw-on animation plays once | no hover state | — | — | Larger size (80px) |

---

## Logo Specification (BRAND-01, BRAND-02, BRAND-03)

| Property | Value |
|----------|-------|
| Concept | "S" letterform that doubles as an infinity/∞ glyph, or continuous loop path |
| Stroke color | `#6366f1` (indigo-500) — SOFIX brand color as primary; can use gradient to rose-500 to represent both platforms |
| Fill | none (stroke-only for draw-on animation) |
| Stroke width | 2–3px at 24px size; scale proportionally |
| Sizes | navbar: 24px height; hero: 80px height; favicon: 16×16px exported as SVG |
| Animation | CSS `stroke-dashoffset` from `pathLength` to `0`; `stroke-dasharray` set to full path length |
| Static export safety | Use `suppressHydrationWarning` on the SVG or inline the animation in a `<style>` block within the SVG — no JS animation |
| Blocker note | STATE.md records: "Design final do logo 'S'/infinity ainda não definido — confirmar se é stroke-based ou fill-based." This UI-SPEC resolves to stroke-based as the locked approach per ROADMAP.md decision: "Logo ecossistema como SVG inline com animação CSS stroke-dashoffset (evita hydration mismatch no static export)" |

---

## Responsive Breakpoints

| Breakpoint | Tailwind | Usage |
|------------|---------|-------|
| Mobile | default (< 640px) | Single column, reduced hero text size |
| sm | `sm:` (≥ 640px) | Two-column CTAs, expanded type sizes |
| lg | `lg:` (≥ 1024px) | Two-column platform card grid |
| Max container | `max-w-5xl` / `max-w-6xl` | Section content width cap |

Responsive preservation is mandatory per HERO-05.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — shadcn not initialized |
| Third-party | none | not applicable |

No third-party component registries are used in this phase. All components are handcrafted using Tailwind CSS utilities and existing project patterns.

---

## Accessibility Contract

| Concern | Requirement | Implementation |
|---------|-------------|----------------|
| Color contrast | WCAG AA — Lighthouse must report no failures (POLISH-06 success criterion) | All body text ≥ `text-gray-400` on `#030712` background; platform accent colors used only as decorative tints, not as sole contrast carriers |
| Reduced motion | POLISH-05 — continuous animations pause | Add `@media (prefers-reduced-motion: reduce)` block to globals.css covering all infinite animation classes |
| SVG accessibility | EcosystemLogo must include `aria-label` or be wrapped with visually hidden text | `<EcosystemLogo aria-label="SOFI Data Ecosystem" />` |
| CTA links | Both hero CTAs must be `<a>` tags (not buttons) linking to platform sections or platform URLs | Already established pattern in HeroSection |
| Focus visible | All interactive elements must show visible focus outline | Use Tailwind `focus-visible:ring-2 focus-visible:ring-indigo-500` or rely on browser default (not `outline: none`) |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

---

## Pre-Population Sources

| Source | Decisions Used |
|--------|---------------|
| `app/globals.css` | Background `#030712`, foreground `#f9fafb`, brand tokens `--sofix-brand` / `--sofi-brand`, all animation class names |
| `app/layout.tsx` | Inter + JetBrains Mono fonts, `lang="pt-BR"` |
| `app/data/platforms.ts` | Indigo/rose platform color system, gradient strings, glow colors, accentColor/accentBorder/accentBg pattern |
| `app/data/stats.ts` | Sublabel field confirms POLISH-04 is already partially implemented |
| `components/sections/HeroSection.tsx` | Existing CTA copy ("Explorar plataformas"), font size/weight classes, stats layout |
| `components/sections/NavBar.tsx` | 64px height, glass morphism pattern, platform link hover colors |
| `components/platform/PlatformCard.tsx` | Hover glow via `boxShadow` + `scale-[1.01]`, gradient border opacity transition |
| `ROADMAP.md` (Phase 2 decisions) | SVG stroke-dashoffset approach, LazyMotion + domAnimation, eyebrow label copy |
| `REQUIREMENTS.md` | BRAND-01–04, HERO-01–05, POLISH-01–06 — all design requirements |
| `STATE.md` | Open blocker on logo final design (resolved: stroke-based per ROADMAP.md decision) |
| User input | 0 (all decisions derived from upstream artifacts and codebase) |
