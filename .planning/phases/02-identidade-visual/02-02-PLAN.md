---
phase: 02-identidade-visual
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - components/sections/PlatformsSection.tsx
  - components/sections/HowItWorksSection.tsx
  - components/sections/EcosystemFeaturesSection.tsx
  - components/platform/PlatformCard.tsx
  - app/data/stats.ts
autonomous: true
requirements: [POLISH-01, POLISH-02, POLISH-03, POLISH-04]

must_haves:
  truths:
    - "PlatformsSection has eyebrow label 'PLATAFORMAS' above the heading"
    - "HowItWorksSection has eyebrow label 'COMO FUNCIONA' above the heading"
    - "EcosystemFeaturesSection has eyebrow label 'ECOSSISTEMA' above the heading"
    - "All informational body text uses text-gray-400 minimum (not text-gray-500 or text-gray-600)"
    - "PlatformCard hover shows border glow using platform glowColor"
    - "All 4 stats have non-empty sublabel strings"
  artifacts:
    - path: "components/sections/PlatformsSection.tsx"
      provides: "Eyebrow label + contrast fix"
      contains: "PLATAFORMAS"
    - path: "components/sections/HowItWorksSection.tsx"
      provides: "Eyebrow label + contrast fix"
      contains: "COMO FUNCIONA"
    - path: "components/sections/EcosystemFeaturesSection.tsx"
      provides: "Eyebrow label + contrast fix"
      contains: "ECOSSISTEMA"
    - path: "components/platform/PlatformCard.tsx"
      provides: "Verified hover glow using platform glowColor"
  key_links:
    - from: "components/platform/PlatformCard.tsx"
      to: "app/data/platforms.ts"
      via: "platform.glowColor used in boxShadow"
      pattern: "platform\\.glowColor"
---

<objective>
Polish section headers with eyebrow labels, fix text contrast to meet WCAG AA, and verify platform card hover glow and stat micro-context lines.

Purpose: These visual refinements give the portal a professional, organized appearance with clear section hierarchy and accessible contrast ratios.

Output: 5 modified files with eyebrow labels, contrast fixes, and verified hover/stat behavior.
</objective>

<execution_context>
@/home/vault-app/portal/.claude/get-shit-done/workflows/execute-plan.md
@/home/vault-app/portal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-identidade-visual/02-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From app/data/platforms.ts:
```typescript
export interface Platform {
  id: string;
  name: string;
  glowColor: string;      // "rgba(99, 102, 241, 0.15)" or "rgba(225, 29, 72, 0.15)"
  accentColor: string;     // "text-indigo-400" or "text-rose-400"
  accentBorder: string;
  accentBg: string;
  // ... other fields
}
```

From app/data/stats.ts:
```typescript
export interface Stat {
  end: number;
  suffix: string;
  label: string;
  sublabel: string;  // Must be non-empty per POLISH-04
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add eyebrow labels to PlatformsSection, HowItWorksSection, and EcosystemFeaturesSection</name>
  <files>components/sections/PlatformsSection.tsx, components/sections/HowItWorksSection.tsx, components/sections/EcosystemFeaturesSection.tsx</files>
  <read_first>
    - components/sections/PlatformsSection.tsx (full current content)
    - components/sections/HowItWorksSection.tsx (full current content)
    - components/sections/EcosystemFeaturesSection.tsx (full current content)
    - .planning/phases/02-identidade-visual/02-UI-SPEC.md (Typography section for eyebrow label classes)
  </read_first>
  <action>
For each of the 3 section files, add an eyebrow label element ABOVE the existing `<h2>` tag, inside the `<div className="text-center mb-14">` wrapper.

The eyebrow label markup (identical pattern for all 3, only text changes):
```jsx
<p className="text-xs font-semibold uppercase tracking-[0.2em] text-gray-500 mb-3">{LABEL}</p>
```

Labels per section:
- PlatformsSection.tsx: `PLATAFORMAS`
- HowItWorksSection.tsx: `COMO FUNCIONA`
- EcosystemFeaturesSection.tsx: `ECOSSISTEMA`

Insert the `<p>` tag as the FIRST child inside `<div className="text-center mb-14">`, before the `<h2>`.

Additionally, fix WCAG AA contrast (POLISH-02) in these 3 files:
- In ALL three sections, the description `<p>` below the `<h2>` currently uses `text-gray-500`. Change it to `text-gray-400` (informational text must be `text-gray-400` minimum on `#030712` background).

Specific changes per file:

**PlatformsSection.tsx:**
- Add `<p className="text-xs font-semibold uppercase tracking-[0.2em] text-gray-500 mb-3">PLATAFORMAS</p>` before the `<h2>`
- Change `<p className="text-gray-500 max-w-lg mx-auto">` to `<p className="text-gray-400 max-w-lg mx-auto">`

**HowItWorksSection.tsx:**
- Add `<p className="text-xs font-semibold uppercase tracking-[0.2em] text-gray-500 mb-3">COMO FUNCIONA</p>` before the `<h2>`
- Change `<p className="text-gray-500 max-w-lg mx-auto">` to `<p className="text-gray-400 max-w-lg mx-auto">`

**EcosystemFeaturesSection.tsx:**
- Add `<p className="text-xs font-semibold uppercase tracking-[0.2em] text-gray-500 mb-3">ECOSSISTEMA</p>` before the `<h2>`
- Change `<p className="text-gray-500 max-w-lg mx-auto">` to `<p className="text-gray-400 max-w-lg mx-auto">`

Note: The eyebrow labels themselves use `text-gray-500` — this is acceptable per UI-SPEC because eyebrow labels are decorative/navigational (Body modifier role), not informational body text. The `text-gray-400` fix applies to the descriptive paragraph text.
  </action>
  <verify>
    <automated>grep -q 'PLATAFORMAS' components/sections/PlatformsSection.tsx && grep -q 'COMO FUNCIONA' components/sections/HowItWorksSection.tsx && grep -q 'ECOSSISTEMA' components/sections/EcosystemFeaturesSection.tsx && grep -c 'text-gray-400' components/sections/PlatformsSection.tsx && grep -c 'text-gray-400' components/sections/HowItWorksSection.tsx && grep -c 'text-gray-400' components/sections/EcosystemFeaturesSection.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - components/sections/PlatformsSection.tsx contains the string `PLATAFORMAS` in a `<p>` with `tracking-[0.2em]`
    - components/sections/HowItWorksSection.tsx contains the string `COMO FUNCIONA` in a `<p>` with `tracking-[0.2em]`
    - components/sections/EcosystemFeaturesSection.tsx contains the string `ECOSSISTEMA` in a `<p>` with `tracking-[0.2em]`
    - All 3 section description paragraphs use `text-gray-400` (not `text-gray-500`)
    - No other existing content is removed or changed in these files
  </acceptance_criteria>
  <done>Three sections display eyebrow labels above their headings, informational text upgraded to text-gray-400 for WCAG AA compliance</done>
</task>

<task type="auto">
  <name>Task 2: Verify and fix PlatformCard hover glow and stat sublabels</name>
  <files>components/platform/PlatformCard.tsx, app/data/stats.ts</files>
  <read_first>
    - components/platform/PlatformCard.tsx (full current content — verify hover glow implementation)
    - app/data/stats.ts (verify all 4 sublabels are non-empty)
    - app/data/platforms.ts (verify glowColor values)
    - .planning/phases/02-identidade-visual/02-UI-SPEC.md (Interaction States section for card hover spec)
  </read_first>
  <action>
**PlatformCard.tsx (POLISH-03):**

The card ALREADY has hover glow via `boxShadow` using `platform.glowColor` and `scale-[1.01]` — verify this is working correctly. The UI-SPEC also requires "elevation" on hover. Add `hover:shadow-2xl` to the card's className to add Tailwind shadow elevation on hover (in addition to the existing glow).

Current card `className` includes:
```
`block group relative rounded-2xl overflow-hidden glass cursor-pointer transition-all duration-500 ${hoveredCard === platform.id ? "scale-[1.01]" : ""}`
```

Change to:
```
`block group relative rounded-2xl overflow-hidden glass cursor-pointer transition-all duration-500 ${hoveredCard === platform.id ? "scale-[1.01] shadow-2xl" : ""}`
```

Also fix contrast on feature labels (POLISH-02): the feature grid items currently use `text-gray-500 group-hover:text-gray-400`. Since these are informational text listing platform features, change the default from `text-gray-500` to `text-gray-400` and the hover to `text-gray-300`:
```
text-[12px] text-gray-400 group-hover:text-gray-300 transition-colors
```

Also fix the description paragraph: it currently uses `text-sm text-gray-400 leading-relaxed mb-4` — this is already correct (text-gray-400). No change needed.

**stats.ts (POLISH-04):**

Read and verify all 4 stat objects have non-empty `sublabel` strings. Current values:
- "SQL, NoSQL, Cloud & SaaS"
- "Zero-copy architecture"
- "vs ETL tradicional"
- "REST, OData, SQL & NLQ"

All are non-empty — no change needed. If any were empty, fill them with a concrete micro-context line. Leave the file as-is since all sublabels are populated.
  </action>
  <verify>
    <automated>grep -q 'shadow-2xl' components/platform/PlatformCard.tsx && grep -q 'text-gray-400 group-hover:text-gray-300' components/platform/PlatformCard.tsx && node -e "const s = require('./app/data/stats.ts'.replace('.ts','')); console.log('sublabels:', s.stats.every(x=>x.sublabel.length>0))" 2>/dev/null; grep -c 'sublabel' app/data/stats.ts | grep -q '5' && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - components/platform/PlatformCard.tsx contains `shadow-2xl` in the hover conditional class
    - components/platform/PlatformCard.tsx contains `text-gray-400 group-hover:text-gray-300` on feature label elements
    - components/platform/PlatformCard.tsx still uses `platform.glowColor` in the `boxShadow` style
    - app/data/stats.ts has 4 stat objects, each with a non-empty `sublabel` string
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Platform cards show elevation + glow on hover with correct platform color, feature text meets WCAG AA contrast, all 4 stats have micro-context sublabels</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0 (static export succeeds)
- `grep 'PLATAFORMAS\|COMO FUNCIONA\|ECOSSISTEMA' components/sections/*.tsx` shows all 3 eyebrow labels
- `grep 'text-gray-500.*max-w-lg' components/sections/PlatformsSection.tsx components/sections/HowItWorksSection.tsx components/sections/EcosystemFeaturesSection.tsx` returns NO matches (all upgraded to gray-400)
- `grep 'shadow-2xl' components/platform/PlatformCard.tsx` returns a match
</verification>

<success_criteria>
- Three main sections show uppercase eyebrow labels above headings
- All informational body text uses text-gray-400 minimum for WCAG AA on dark background
- Platform cards display elevation (shadow-2xl) plus border glow (platform.glowColor) on hover
- All 4 stat counters have non-empty micro-context sublabels
- Static export builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-identidade-visual/02-02-SUMMARY.md`
</output>
