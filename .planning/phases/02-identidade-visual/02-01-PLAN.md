---
phase: 02-identidade-visual
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ui/EcosystemLogo.tsx
  - components/ui/LazyMotionProvider.tsx
  - app/globals.css
  - public/favicon.svg
autonomous: true
requirements: [BRAND-01, BRAND-02, BRAND-03, BRAND-04, POLISH-05]

must_haves:
  truths:
    - "Ecosystem logo renders as an SVG 'S'/infinity shape with indigo stroke color"
    - "Logo draw-on animation plays once on mount via CSS stroke-dashoffset"
    - "Logo accepts size prop and renders at navbar (24px), hero (80px), and favicon sizes"
    - "LazyMotionProvider wraps LazyMotion + domAnimation from framer-motion"
    - "prefers-reduced-motion media query pauses all continuous animations in globals.css"
    - "Logo shows static stroked state when reduced motion is preferred"
  artifacts:
    - path: "components/ui/EcosystemLogo.tsx"
      provides: "SVG ecosystem logo with draw-on animation"
      exports: ["EcosystemLogo"]
    - path: "components/ui/LazyMotionProvider.tsx"
      provides: "LazyMotion wrapper for framer-motion"
      exports: ["LazyMotionProvider"]
    - path: "app/globals.css"
      provides: "prefers-reduced-motion CSS block"
      contains: "@media (prefers-reduced-motion: reduce)"
    - path: "public/favicon.svg"
      provides: "Ecosystem logo as favicon"
  key_links:
    - from: "components/ui/EcosystemLogo.tsx"
      to: "app/globals.css"
      via: "CSS keyframe for stroke-dashoffset draw-on"
      pattern: "logo-draw"
    - from: "components/ui/LazyMotionProvider.tsx"
      to: "framer-motion"
      via: "LazyMotion + domAnimation import"
      pattern: "LazyMotion.*domAnimation"
---

<objective>
Create the SOFI Data Ecosystem visual identity foundation: the SVG logo component with draw-on animation, the LazyMotion provider for future framer-motion usage, the favicon, and the prefers-reduced-motion accessibility CSS.

Purpose: These are the building blocks that Plan 03 (hero redesign) and future phases will consume. The logo and LazyMotionProvider must exist before they can be integrated into NavBar and HeroSection.

Output: 4 new/modified files providing the brand identity primitives.
</objective>

<execution_context>
@/home/vault-app/portal/.claude/get-shit-done/workflows/execute-plan.md
@/home/vault-app/portal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-identidade-visual/02-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create EcosystemLogo component and favicon</name>
  <files>components/ui/EcosystemLogo.tsx, public/favicon.svg</files>
  <read_first>
    - app/globals.css (existing animation patterns to follow)
    - components/ui/Section.tsx (existing component pattern — "use client", export style)
    - .planning/phases/02-identidade-visual/02-UI-SPEC.md (Logo Specification section)
  </read_first>
  <action>
Create `components/ui/EcosystemLogo.tsx`:

1. Define a React component that renders an inline SVG of an "S" shape that visually resembles an infinity/continuous-loop glyph. The SVG should use a single `<path>` element with `fill="none"` and `stroke="#6366f1"` (indigo-500, the SOFIX brand color).

2. Accept a `size` prop with three values: `"navbar"` (24px height), `"hero"` (80px height), `"favicon"` (16px height). Use a simple mapping object:
   ```
   const sizes = { navbar: 24, hero: 80, favicon: 16 } as const;
   ```
   Set the SVG `width` and `height` from this mapping. Use a `viewBox` of `"0 0 60 32"` (wide S/infinity shape).

3. The SVG path should be an S-curve / infinity loop. Example path data for an S/infinity hybrid:
   ```
   M8,24 C8,24 8,16 16,16 C24,16 24,8 24,8 C24,8 24,0 32,0 C40,0 40,8 40,8 C40,8 40,16 48,16 C56,16 56,24 48,24 C40,24 40,16 32,16 C24,16 24,24 16,24 C8,24 8,24 8,24
   ```
   (Adjust control points for visual appeal — the executor should refine this to look like a continuous S/infinity loop that is recognizable at 24px.)

4. Add `stroke-width` that scales: `2` at viewBox scale (will look proportional at all sizes).

5. For the draw-on animation (BRAND-02), use CSS class `logo-draw` on the path element. Set `stroke-dasharray` and `stroke-dashoffset` via inline style to the path's total length (calculate or estimate — use `pathLength="1"` attribute on the path and then `stroke-dasharray="1"` and `stroke-dashoffset="1"` for simplicity). The CSS animation `logo-draw-on` will animate `stroke-dashoffset` from 1 to 0.

6. Add `aria-label="SOFI Data Ecosystem"` on the SVG element. Add `role="img"`.

7. For `prefers-reduced-motion`: add a second CSS class `logo-draw-static` that the component uses instead when `prefers-reduced-motion: reduce` is active. Since this is SSR-safe (static export), use the CSS approach: always apply the animation class, and let the CSS media query override it to show the static state (stroke-dashoffset: 0, no animation).

8. Do NOT use `"use client"` — this component is pure SVG + CSS, no hooks needed. It can render in Server Components.

9. Export as named export: `export function EcosystemLogo({ size = "navbar" }: { size?: "navbar" | "hero" | "favicon" })`.

Create `public/favicon.svg`:

10. Create a standalone SVG file with the same S/infinity path, viewBox `"0 0 60 32"`, stroke `#6366f1`, fill `none`, stroke-width `3` (slightly thicker for favicon legibility). No animation classes — just the static shape.
  </action>
  <verify>
    <automated>test -f components/ui/EcosystemLogo.tsx && test -f public/favicon.svg && grep -q 'stroke="#6366f1"' components/ui/EcosystemLogo.tsx && grep -q 'aria-label' components/ui/EcosystemLogo.tsx && grep -q 'export function EcosystemLogo' components/ui/EcosystemLogo.tsx && grep -q 'svg' public/favicon.svg && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - components/ui/EcosystemLogo.tsx contains `export function EcosystemLogo`
    - components/ui/EcosystemLogo.tsx contains `stroke="#6366f1"`
    - components/ui/EcosystemLogo.tsx contains `aria-label="SOFI Data Ecosystem"`
    - components/ui/EcosystemLogo.tsx contains `role="img"`
    - components/ui/EcosystemLogo.tsx contains `pathLength="1"`
    - components/ui/EcosystemLogo.tsx contains `logo-draw` (CSS class reference)
    - components/ui/EcosystemLogo.tsx contains `sizes` mapping with `navbar: 24`, `hero: 80`, `favicon: 16`
    - components/ui/EcosystemLogo.tsx does NOT contain `"use client"`
    - public/favicon.svg contains `<svg` and `stroke="#6366f1"`
  </acceptance_criteria>
  <done>EcosystemLogo component renders at 3 sizes with draw-on animation class, favicon.svg exists as static SVG</done>
</task>

<task type="auto">
  <name>Task 2: Create LazyMotionProvider, add logo draw-on keyframe and reduced-motion CSS</name>
  <files>components/ui/LazyMotionProvider.tsx, app/globals.css</files>
  <read_first>
    - app/globals.css (must append to existing file, not overwrite)
    - components/ui/EcosystemLogo.tsx (created in Task 1 — verify the CSS class name used)
    - .planning/phases/02-identidade-visual/02-UI-SPEC.md (Animation Contract and LazyMotion Setup sections)
  </read_first>
  <action>
Create `components/ui/LazyMotionProvider.tsx`:

1. This IS a client component — add `"use client"` at top.
2. Import `LazyMotion` and `domAnimation` from `"framer-motion"`.
3. Export a named component:
   ```typescript
   "use client";

   import { LazyMotion, domAnimation } from "framer-motion";

   export function LazyMotionProvider({ children }: { children: React.ReactNode }) {
     return <LazyMotion features={domAnimation}>{children}</LazyMotion>;
   }
   ```
4. This is the ONLY file that should import from `framer-motion` directly. All other components should import `m` from `framer-motion` for the animated elements, wrapped inside this provider.

Modify `app/globals.css` — APPEND (do not remove or change existing content):

5. Add the logo draw-on keyframe after the existing `/* -- Animations -- */` section:
   ```css
   @keyframes logo-draw-on {
     from { stroke-dashoffset: 1; }
     to { stroke-dashoffset: 0; }
   }

   .logo-draw {
     stroke-dasharray: 1;
     stroke-dashoffset: 1;
     animation: logo-draw-on 1.5s ease-out forwards;
   }
   ```

6. Add the `prefers-reduced-motion` block at the END of the file (after the scrollbar section):
   ```css
   /* -- Reduced Motion -- */

   @media (prefers-reduced-motion: reduce) {
     .animate-pulse-glow,
     .animate-float,
     .animate-float-delayed,
     .animate-gradient,
     .animate-shimmer {
       animation: none !important;
     }

     .status-dot::before {
       animation: none !important;
     }

     .logo-draw {
       animation: none !important;
       stroke-dashoffset: 0;
     }

     .animate-fade-in-up,
     .animate-fade-in-up-1,
     .animate-fade-in-up-2,
     .animate-fade-in-up-3,
     .animate-fade-in-up-4 {
       animation: none !important;
       opacity: 1;
     }
   }
   ```
  </action>
  <verify>
    <automated>grep -q 'logo-draw-on' app/globals.css && grep -q 'prefers-reduced-motion: reduce' app/globals.css && grep -q 'animate-pulse-glow' app/globals.css && grep -q 'LazyMotion' components/ui/LazyMotionProvider.tsx && grep -q 'domAnimation' components/ui/LazyMotionProvider.tsx && grep -q '"use client"' components/ui/LazyMotionProvider.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - components/ui/LazyMotionProvider.tsx contains `"use client"`
    - components/ui/LazyMotionProvider.tsx contains `import { LazyMotion, domAnimation } from "framer-motion"`
    - components/ui/LazyMotionProvider.tsx contains `export function LazyMotionProvider`
    - app/globals.css contains `@keyframes logo-draw-on`
    - app/globals.css contains `.logo-draw {` with `stroke-dasharray: 1` and `animation: logo-draw-on 1.5s ease-out forwards`
    - app/globals.css contains `@media (prefers-reduced-motion: reduce)` block
    - Reduced motion block covers: `.animate-pulse-glow`, `.animate-float`, `.animate-float-delayed`, `.animate-gradient`, `.animate-shimmer`, `.status-dot::before`, `.logo-draw`, `.animate-fade-in-up`
    - All existing globals.css content is preserved (`:root` tokens, `.glass`, `.status-dot`, all existing keyframes)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>LazyMotionProvider component exists, logo draw-on animation keyframe defined in globals.css, prefers-reduced-motion block covers all continuous animations, build passes</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0 (static export succeeds)
- `grep -c 'export function' components/ui/EcosystemLogo.tsx` returns 1
- `grep -c 'export function' components/ui/LazyMotionProvider.tsx` returns 1
- `grep -c 'prefers-reduced-motion' app/globals.css` returns at least 1
- `public/favicon.svg` exists and contains SVG markup
</verification>

<success_criteria>
- EcosystemLogo renders an S/infinity SVG at 3 sizes (24px, 80px, 16px) with draw-on animation via CSS
- LazyMotionProvider wraps LazyMotion + domAnimation for future use by motion components
- prefers-reduced-motion pauses all continuous animations including logo draw-on
- favicon.svg replaced with ecosystem logo
- Static export builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-identidade-visual/02-01-SUMMARY.md`
</output>
